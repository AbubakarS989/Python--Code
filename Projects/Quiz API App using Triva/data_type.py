# we can declared data type in also this way to use it later in program

name: str
clas : int
percentage: float

#! we can also specify data type in functions to be-aware from wrong entry
def sum(a:int ,b: int ):
    return a+b

sum(2,3)


# ! specify the output of a function
def minus(a:int,b:int) -> int:
    return a-b

min(3,4)