# Evaluate
# eval()
# could be dangerous not run it


#  we can execute some line of code.
# just input lines of code and eval() evaluate it as areal code
# in this function we are not allowed to assign variables

code=input("Enter Some code:")
exit()
eval(code)