# Execution
# exec()
# could be dangerous not run it


# We can execute some line of code.
# just input lines of code and eval() evaluate it as a real code
# in this function we are  allowed to assign variables

# Format:
#  a=1; print(a);
# b=0; b+=1; print(b)
code=input("Enter Some code:")
exit()
exec(code)